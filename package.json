{
  "name": "investigator",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -c auto \"vite build --watch\" \"vite build --config vite.bridge.config.ts --watch\" \"vite build --config vite.main-world.config.ts --watch\"",
    "build": "tsc --noEmit && vite build && vite build --config vite.bridge.config.ts && vite build --config vite.main-world.config.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview",
    "pack": "npm run build && node scripts/pack-extension.mjs",
    "pack:zip": "node scripts/pack-extension.mjs"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.2.1",
    "@types/chrome": "^0.0.287",
    "@types/node": "^25.7.0",
    "concurrently": "^9.2.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.6",
    "vitest": "^3.1.3"
  }
}
